# Static search with Pagefind

EggDoc includes site search from the initial build, using a static search index rather than a backend search service. Pagefind fits the static Astro and Cloudflare Pages architecture while keeping article, guide, lesson, and note discovery usable as the content library grows.
