# No initial versioned docs

EggDoc does not implement versioned documentation at launch. Articles show published and updated dates to communicate freshness, and versioned sections such as `/eggai/v2/` should only be introduced if real service or API version differences make them necessary.
