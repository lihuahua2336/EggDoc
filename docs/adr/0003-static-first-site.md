# Static-first site

EggDoc is designed as a static-first documentation and content site: Markdown and MDX content is rendered at build time into static pages and deployed behind a CDN-style host. Runtime server features such as accounts, submissions, comments, or database-backed content are out of scope until the product need clearly appears.
