# Draft and featured content flags

EggDoc supports a `draft` flag to keep unfinished content out of production builds and a separate `featured` flag for homepage or hub promotion. Publication state and editorial prominence should remain separate concepts so navigation choices do not accidentally control whether an article is public.
