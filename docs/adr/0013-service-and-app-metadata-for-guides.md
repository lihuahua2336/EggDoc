# Service and app metadata for guides

EggDoc guide content can carry service and app metadata, such as `service: eggai` and `app: cursor`, to support focused guide hubs and cross-cutting app pages. These fields should support discovery and related content without making products the only organizing principle of the site.
