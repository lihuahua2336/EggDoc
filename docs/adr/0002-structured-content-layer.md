# Structured content layer

EggDoc uses a structured content layer over raw Markdown and MDX files instead of ad hoc file scanning in page components. The content layer should validate frontmatter, expose typed article metadata, and support navigation, listing, sorting, and search indexing as first-class site concerns.
