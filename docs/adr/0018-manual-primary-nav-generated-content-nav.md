# Manual primary nav, generated content nav

EggDoc uses a manually curated primary navigation and generates content-specific navigation from structured metadata. Top-level navigation expresses the site's editorial priorities, while guide lists, learning path chapters, table of contents, previous and next links, tags, and related content should come from the content layer.
