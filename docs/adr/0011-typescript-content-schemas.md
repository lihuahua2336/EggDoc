# TypeScript content schemas

EggDoc uses TypeScript throughout the project and validates content frontmatter with typed schemas. This makes article metadata, guide ordering, learning path structure, search indexing, and SEO fields explicit instead of relying on informal Markdown conventions.
