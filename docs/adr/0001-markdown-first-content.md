# Markdown-first content

EggDoc stores articles as Markdown by default and allows MDX only when an article needs richer presentation such as interactive examples, reusable callouts, embeds, or comparison components. This keeps most content portable and easy to maintain while preserving an escape hatch for tutorials that benefit from React-powered presentation.
