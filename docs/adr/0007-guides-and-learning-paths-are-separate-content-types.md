# Guides and learning paths are separate content types

EggDoc separates task-oriented guide collections from sequenced learning paths. The EggAi integration material belongs in a guide collection because readers usually arrive with a specific setup or troubleshooting task, while the AI programming course belongs in a learning path because it is meant to be followed in order.
