# Delegate identity to EggAi Logto

EggDoc delegates registration and authentication to EggAi through Logto instead of maintaining its own account credentials. All published content remains public; an authenticated EggDoc session exists only to retrieve personalized EggAi configuration, avoiding duplicate accounts and password-management responsibilities while requiring a small server-side authentication boundary.
